import Navigation from '../Navigation';
import UserMenu from '../UserMenu';

const Appbar = () => {
  return (
    <header>
      <Navigation />
      <UserMenu />
    </header>
  );
};

export default Appbar;
